import React from "react";

const Checkout = () => {
  return (
    <div>
      checkout checkout checkout checkout checkout checkout checkout checkout
      checkout checkout
    </div>
  );
};

export default Checkout;
